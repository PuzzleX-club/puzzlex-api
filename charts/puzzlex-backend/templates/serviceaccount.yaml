{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "puzzlex-backend.serviceAccountName" . }}
  labels:
    {{- include "puzzlex-backend.labels" . | nindent 4 }}
{{- end -}}
